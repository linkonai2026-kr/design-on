---
name: designon
description: 한 문장 요청에서 완성된 웹페이지 HTML을 만든다. 컬러 팔레트 78세트와 디자인 툴 883개 DB를 내장하고, AI 티가 나지 않는 레이아웃·카피 규칙을 적용한다. "카페 사이트 만들어줘", "필라테스 스튜디오 페이지", "세무사무소 홈페이지" 같은 웹페이지·랜딩페이지 제작 요청에 쓴다. 색상 팔레트 추천이나 업종별 디자인 툴 조회에도 쓴다.
---

# designon

사용자가 업종과 원하는 느낌을 한 문장으로 말하면, 컬러·서체·사진·카피를 전부 정해서 완성된 단일 HTML 페이지를 만든다.

## 실행 방법

**이 스킬 디렉토리의 `PLAYBOOK.md`를 읽고 그 절차를 그대로 따른다.** 전체 방법론이 거기 있다.

```
{이 스킬 폴더}/PLAYBOOK.md
```

핵심 원칙만 미리 요약하면 이렇다.

- 중간에 되묻지 않는다. 미지정 항목은 업종 통념으로 직접 정한다.
- 팔레트는 `data/palettes.json`에서 고르고 대비 4.5:1을 계산해 검증한다.
- 사진은 실행 환경의 네이티브 이미지 생성 → Gemini API → 실사 스톡 → 타이포그래피 주도 순으로 폴백한다.
- lazyweb·emil-design-eng·ui-skills가 설치돼 있으면 자동으로 위임하고, 없으면 건너뛴다.
- 보라→파랑 그라디언트 히어로, 이모지 아이콘 3단 카드, 번역투 카피는 쓰지 않는다.
- 결과는 `site/index.html`에 저장하고 교체할 예시 정보를 알려준다.

## 데이터

| 파일 | 내용 |
|---|---|
| `data/palettes.json` | 컬러 팔레트 78세트, HEX 369개 |
| `data/tools.json` | 디자인 툴·사이트 883개. `seen` 3 이상이면 반복 검증된 툴 |
