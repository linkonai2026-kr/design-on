# design-on v2 결정 기록

## 왜 v1은 AI 티가 났나

v1 PLAYBOOK에는 "금지 목록"이 있었다. 그런데 금지 목록만으로는 안 된다. 이유가 세 가지다.

**1. 검증이 없었다.** 규칙을 어겨도 아무도 잡아주지 않는다. 에이전트가 자기 규칙을 지켰는지 자기가 판단하면, 지켰다고 답한다. → 기계 검사(Impeccable 디텍터)를 도입해 **어겼는지 아닌지를 사람 판단 밖으로** 뺐다.

**2. 디자인 방향을 정하는 단계가 없었다.** v1은 팔레트 고르고 바로 HTML로 갔다. 10년차 디자이너는 그렇게 일하지 않는다. 먼저 "이 페이지가 어떤 세계인지"를 정하고, 그 세계에 맞춰 색·서체·여백·모션을 전부 종속시킨다. → tasteskill의 브리프 추론 + 3다이얼, Impeccable의 mode(Persuade/Operate/Read/Experience)를 STEP 2에 넣었다.

**3. 순차 실행이라 뒤로 갈수록 흐려졌다.** 한 에이전트가 리서치→팔레트→사진→카피→빌드→검수를 다 하면, 뒤쪽 작업은 앞쪽 맥락에 눌려 대충 처리된다. → 제작 4트랙, 검수 3트랙을 **병렬 서브에이전트**로 분리했다. 각 트랙은 자기 일만 신경 쓴다.

## v1 기본값 자체가 AI 티였다

디텍터를 돌려보고 알게 된 것이다. v1이 권장하던 값들이 그대로 안티패턴이었다.

| v1 권장 | 디텍터 판정 | v2 대응 |
|---|---|---|
| 크림·웜 오프화이트 배경 | `cream-palette` — "안전한 웜 오프화이트로 반사적으로 도망친 배경" | 팔레트에서 유래한 배경만 허용. 크림은 근거가 있을 때만 |
| Pretendard 단독 | `single-font` — 한 서체로 전체를 처리 | 본문/제목 서체 페어링 강제 |
| 보라 액센트 | `ai-color-palette` | **브리프가 이긴다.** 사용자가 보라를 요청하면 유지하고, 요청 없이 보라로 가면 다시 고른다 |

세 번째가 중요하다. 사용자의 대표 예시가 "보라색 카페 사이트"인데 디텍터는 보라를 경고한다. Impeccable 자체 문서의 원칙("The brief wins")을 그대로 채택해서, **명시적 요청은 경고를 무효화**하고 그 사실을 보고에 남기게 했다.

## 왜 벤더링했나 (설치 스크립트가 아니라)

사용자 요구는 "한 번에 다 되게"였다. 선택지는 둘이었다.

- **A. 공식 인스톨러 호출** — 항상 최신, 업스트림에 트래픽이 간다. 대신 네트워크·npx가 필요하고 5개 중 하나라도 실패하면 약속이 깨진다.
- **B. 벤더링** — 저장소만 받으면 끝. 대신 재배포이므로 라이선스를 지켜야 하고 언젠가 낡는다.

**B를 골랐다.** 4종 전부 MIT/Apache-2.0이라 저작권·라이선스 고지를 보존하면 재배포가 허용된다. `.upstream-commit`에 원본 커밋 해시를 남기고, `CREDITS.md`에 출처를 명시하고, `scripts/update-vendor.sh`로 재동기화 경로를 뒀다. 낡는 문제는 이걸로 관리한다.

**lazyweb만 제외했다.** MIT지만 실제 기능이 호스팅 API + 발급 토큰에 있다. SKILL.md만 복사해봐야 토큰이 없어 동작하지 않는다. 껍데기를 넣어놓고 "포함됨"이라고 하는 게 더 나쁘다. 런타임에 감지해서 있으면 쓰고, 없으면 웹 검색으로 대체한다.

## 디텍터를 오프라인으로 돌리는 법

Impeccable 스킬에 딸린 번들 디텍터(`scripts/detect.mjs`)는 파서 4종(`htmlparser2`, `css-select`, `css-tree`, `domutils`)이 없으면 **조용히 축소 모드로 떨어진다.** 정규식 엔진만 돌아서 같은 파일에서 26건 → 4건으로 줄어든다. 경고도 안 띄운다.

실측으로 확인한 사실이다.

| 조건 | 검출 |
|---|---|
| 파서 없음 | 4건 (gradient-text, side-tab, dark-glow, overused-font) |
| 파서 있음 | 26건 (+ 대비, 중첩카드, 크림배경, 헤딩건너뜀, 회색-온-컬러 …) |

그래서 `vendor/impeccable/package.json`을 로컬 의존성 앵커로 두고 `npm install --prefix vendor/impeccable`로 파서를 그 트리 안에 심었다. ESM `import()`는 호출한 파일에서 위로 올라가며 `node_modules`를 찾기 때문에 이 위치여야 한다. `NODE_PATH`로는 안 된다(테스트해봤고 실패했다).

`scripts/slopcheck.mjs`는 파서 존재를 먼저 확인하고, 없으면 **축소 모드임을 명시적으로 경고**한다. 조용한 degrade가 제일 위험하다.

## 병렬 트랙을 어떻게 나눴나

기준은 "서로 결과를 기다리지 않아도 되는가"다.

**제작 4트랙** — 전부 STEP 2에서 확정한 아트 디렉션 카드 하나만 입력으로 받는다. 서로 참조하지 않는다.

- 리서치(섹션 구조·업종 관용) / 카피(한국어 원문) / 사진(생성·조달) / 모션(1개 authored moment)

**검수 3트랙** — 전부 완성된 `site/index.html` 하나만 읽는다. 서로 참조하지 않는다.

- 기계 검사(디텍터) / 크래프트 검수(Impeccable craft-floor) / 한국어 카피 검수

팔레트 확정만 병렬에서 뺐다. 나머지 전 트랙이 색상값을 필요로 하므로 STEP 2에서 직렬로 끝내야 한다.

## 멀티페이지를 왜 나중에 넣었나

v2 초기 설계는 "단일 HTML 파일"을 기술 요건에 못 박아뒀다. 이유가 있긴 했다. 소상공인이 파일 하나만 들고 다니면 되고, 더블클릭하면 열리고, 카톡으로 보내기도 쉽다.

그런데 **기본값이어야 할 것을 제약으로 써놨다는 게 문제였다.** 시술이 15종이고 각각 과정·주의사항·가격이 붙는 미용실을 원페이지에 넣으면 스크롤이 끝없이 길어진다. 작품 갤러리와 작품별 상세가 나뉘는 공방도 마찬가지다. 사용자가 "나눠줘"라고 해도 규칙이 막고 있었다.

그래서 **기본은 원페이지, 판단 근거를 명시하고, 요청하면 무조건 나눈다**로 바꿨다. 판단은 STEP 1-2에서 하고, 결과를 아트 디렉션 카드의 `구성` 항목에 실어 병렬 트랙에 넘긴다. 리서처와 카피라이터가 이 값을 보고 산출물 단위를 정한다.

질문 개수 제한(3개 이하)이 걸림돌이었다. 색·분위기·목적·상호가 이미 자리를 차지한다. 그래서 **페이지 구성은 원칙적으로 묻지 않고 요청에서 감지한다.** 명시 요청이 있으면 그냥 나누고, 진짜 애매할 때만 질문 하나로 끼워 넣는다.

## 멀티페이지에서 검사가 약해지지 않는지 먼저 확인했다

멀티페이지로 가면 CSS를 파일로 뽑아야 한다(페이지마다 `<style>`을 복제하면 색 하나 고치는 데 다섯 군데를 고쳐야 하고 반드시 어긋난다). 그런데 **디텍터가 `<link rel="stylesheet">`를 따라가지 못하면 검사가 통째로 무력화된다.** 인라인 CSS만 보던 것이 갑자기 아무것도 못 보게 되는 것이다.

규칙을 쓰기 전에 이것부터 실측했다. 외부 CSS 파일 하나를 공유하는 2페이지짜리 테스트를 만들어 돌렸더니 `cream-palette`, `low-contrast`, `side-tab`, `skipped-heading`이 **양쪽 페이지 모두에서** 잡혔다. 따라간다.

그다음 확인한 것은 **안쪽 페이지가 실제로 스캔되는가**다. 폴더를 넘기면 전부 도는 것처럼 보여도, 실제로는 첫 파일만 보고 있을 수 있다. 마지막 페이지(`visit.html`)에만 눈썹 라벨을 심어보니 그 파일 경로와 함께 `hero-eyebrow-chip`이 잡혔다. 스캔된다.

이 두 가지가 확인되지 않았으면 멀티페이지를 "지원한다"고 쓸 수 없었다. 검사가 안 되는 출력 형식을 늘리는 건 v1으로 돌아가는 것이다.

## 브라우저 측정에서 걸린 함정

`examples/salon/`을 미리보기로 열고 `document.documentElement.scrollWidth > clientWidth`로 가로 오버플로를 쟀더니 `true`가 나왔다. 그런데 같이 찍힌 `clientWidth`가 `0`이었다.

미리보기 패널이 레이아웃 폭을 안 잡은 상태였고, 폭 0에서는 모든 요소가 "넘친다"고 나온다. 뷰포트를 1280과 375로 명시적으로 지정하고 다시 재니 양쪽 다 오버플로 없음이었다.

**측정값을 읽을 때 기준값이 유효한지 같이 확인해야 한다.** `clientWidth: 0`을 안 봤으면 없는 버그를 잡겠다고 CSS를 뜯어고칠 뻔했다.

## 검수 루프를 1회로 제한한 이유

Impeccable SKILL.md의 원칙을 그대로 가져왔다. "Verify in bounded passes, not a loop." 열린 자가 QA는 토큰만 태우고 결과는 나빠진다. 그래서 **검수 → 일괄 수정 → 재검사 1회 → 종료**로 못 박았다. 재검사에서 남은 게 있으면 고치지 말고 보고한다.

---

# v4 — 만들고 끝내지 않기

## 원래 방향과 대조해봤더니 마지막 한 칸이 비어 있었다

사용자가 처음 말한 방향은 이거였다. *"일반인이나 바이브 코딩을 처음 하는 사람도 다운받아 실행시키면 몇 가지 질문을 하고, 그다음 어울리는 걸 내가 넣어둔 것 중에서 찾아서 조화롭게 한 번에 완성시키는 것."*

다섯 조각으로 쪼개서 현재 구현과 대조했다.

| 원래 방향 | 현재 | |
|---|---|---|
| 다운받아 실행 | clone → npm → setup → 플러그인 등록(방법 안 적혀 있음) → 명령어 입력 | ✗ |
| 몇 가지 질문 | STEP 1-4. 팔레트 먼저 조회하고 근거 있는 3개 이하 | ✓ |
| 넣어둔 것 중에서 찾아서 | `pick.mjs`로 팔레트 78·서체 9·사진 레시피 7·툴 883 조회 | ✓ |
| 조화롭게 | 아트 디렉션 카드 하나를 4트랙이 공유 | ✓ |
| **한 번에 완성** | **상호명·전화번호·주소가 전부 지어낸 값** | ✗ |

가운데 셋은 맞았다. 어긋난 건 양 끝이었고, 그중 심각한 건 마지막이다.

## `<!-- 실제 정보로 교체하세요 -->`가 설계 실패였다

v3까지 STEP 7의 마지막 산출물은 "교체해야 할 예시 정보 목록"이었다. 그럴듯해 보이지만 실제로 무슨 일이 벌어지냐면 — **HTML을 만지기 싫어서 이 도구를 받은 사람에게 HTML을 만지라고 시킨다.** 90% 완성 + 숙제다.

더 나쁜 건 그 숙제가 제일 어려운 부분이라는 점이다. 색을 고르는 건 대신 해주고, 태그 안의 텍스트를 찾아 바꾸는 건 본인더러 하라는 것이다. 순서가 거꾸로다.

그래서 **STEP 8**을 만들었다. 다 만든 다음에 물어보고, 받은 값을 직접 넣는다.

## "루프 금지"와 "사용자에게 묻기"를 구분해야 했다

여기서 정면으로 걸리는 게 있었다. PLAYBOOK STEP 6에 **"루프를 돌지 마라"**가 박혀 있고, AGENTS.md에도 "답을 받으면 더 묻지 말고 끝까지 만든다"가 있다. 그대로 두면 STEP 8이 자기 규칙을 위반한다.

Impeccable 원문(`Verify in bounded passes, not a loop`)이 금지하는 건 **에이전트가 혼자 자기 결과를 재검토하는 루프**다. 그건 추측 위에 추측을 쌓아서 토큰만 태운다. **사람에게 묻는 건 정반대다. 추측을 사실로 바꾸는 유일한 방법이고 토큰은 거의 안 든다.**

둘을 구분하는 문단을 STEP 8 머리에 명시적으로 넣었다. 안 넣으면 다음 세션의 에이전트가 "루프 금지"를 읽고 STEP 8을 건너뛴다. 실제로 v3까지가 그랬다.

다만 무한정 묻는 것도 같은 실패라 **최대 2라운드**로 잘랐다.

## 실제 정보를 STEP 1이 아니라 STEP 8에서 받는 이유

상호명·전화번호를 처음부터 물으면 되지 않나 — 두 가지 이유로 안 된다.

첫째, **STEP 1은 3개 이하 질문 예산이 이미 꽉 찼다.** 색·분위기·목적으로 다 쓴다. 여기에 전화번호·주소·영업시간·메뉴를 붙이면 시작하기도 전에 설문지가 된다.

둘째, **아직 뭐가 필요한지 모른다.** "인스타 계정 있으세요?"는 페이지에 인스타 링크를 넣었을 때만 의미가 있다. 만들기 전에는 어떤 항목이 들어갈지 확정되지 않는다. 그래서 STEP 8-2에 **"페이지에 실제로 들어간 가짜 값만 묻는다"**를 규칙으로 박았다.

부수 효과로 사용자 경험도 낫다. 화면을 본 다음에 "여기 전화번호 자리에 뭘 넣을까요"를 들으면 구체적이지만, 빈손일 때 같은 질문을 받으면 막연하다.

## 사진은 뭉뚱그려 물으면 안 된다

`"사진 마음에 드세요?"`는 답할 수 없는 질문이다. 어느 사진인지 모르고, 뭐가 문제인지 말하려면 사용자가 스스로 어휘를 찾아야 한다.

그래서 **자리마다 지금 뭐가 보이는지 한 줄로 적고 번호를 매긴다.** 그리고 바꾸겠다고 하면 **어떻게 바꿀지 먼저 묻고 나서** 손댄다. 마음대로 다른 사진을 넣고 "바꿨습니다"라고 하는 게 제일 나쁘다 — 사용자는 뭐가 달라질지 모른 채 한 번 더 확인해야 한다.

선택지를 네 개로 고정했다. 분위기(a) / 소재(b) / 직접 찍은 사진(c) / 아예 제거(d). d를 넣은 이유는 **사진을 빼는 것도 정답일 때가 있어서**다. 그때는 빈자리를 남기지 말고 STEP 3 트랙 C의 타이포 주도로 그 섹션을 다시 짠다.

## 방향이 틀렸으면 HTML을 고치면 안 된다

8-1에서 "느낌이 아니다"가 나오면 세부를 고쳐봐야 소용없다. 색이 틀렸는데 여백을 조정하는 꼴이다. **STEP 2 아트 디렉션 카드로 되감아서** 팔레트·모드·다이얼을 다시 정하고 STEP 4부터 다시 돈다. 카피는 대부분 살아남으니 STEP 3을 통째로 다시 돌리지는 않는다.

되감기는 **1회로 제한**했다. 두 번째도 아니라고 하면 그 시점에는 추측이 틀린 것이고, 세 번째 추측을 만드는 건 낭비다. 그때는 레퍼런스 URL을 달라고 한다.

## Astryx — 제외 사유가 틀렸었다

v3에서 "빌드 도구가 필요해서 단일 HTML 약속이 깨진다"고 제외했다. 사용자가 **"꼭 HTML로 한정짓지 않아도 된다"**고 하면서 그 논거가 통째로 무효가 됐다. 다시 열어봤다.

문서를 재확인한 사실. 패키지 3종 필수(`core`·`theme-neutral`·`cli`), 빌드 필수, CDN 배포 없음, 토큰은 CSS 커스텀 프로퍼티, 테마 7종(neutral·butter·chocolate·gothic·matcha·stone·y2k), 컴포넌트는 React 전용. `@astryxdesign/core@0.1.9`의 exports 2,474개가 전부 `dist/*.js`·`*.d.ts`라는 이전 실측도 그대로다.

**진짜 제외 사유는 목적이 반대라는 것이다.** Astryx는 하나의 제품을 일관되게 만들기 위한 시스템이다. 버튼·간격·모서리·타입스케일이 정해져 있어서 어느 화면을 만들어도 같은 제품처럼 보인다. design-on은 정반대를 원한다 — 카페와 도자기 공방과 쇼핑몰이 같은 버튼을 쓰면 그게 바로 "AI가 만든 티"고, 안티패턴 디텍터까지 붙여서 잡으려는 게 그것이다. **슬롭을 잡으려고 만든 도구에 슬롭 발생기를 다는 셈이 된다.**

이 논리가 그대로 React 규칙 1번이 됐다. **React로 가도 MUI·Chakra·shadcn을 깔지 않는다.** 같은 이유다.

테마 7종은 `palettes.json` 78세트가 이미 더 넓게 커버한다.

## React를 허용하되 조건을 좁힌 이유

사용자가 "react를 사용해도 돼"라고 했다. 그런데 React는 **수단으로 지목된 것이지 목표가 아니다.** 실제 목표는 "화려해지고 인터랙티브해도 된다"였다.

이 둘을 분리해서 받았다.

- 화려함·인터랙티브 → **STEP 2-6-1 레벨(CSS / Canvas / Three.js CDN / Spline)로 이미 된다.** 여전히 더블클릭으로 열린다. `examples/studio/`가 Three.js 3D 히어로를 그렇게 넣었다
- React → **화면에 상태가 여러 개 얽힐 때만.** 다중 조건 필터, 뒤로 가도 입력이 남는 단계형 폼, 로그인 분기

React로 가는 순간 사용자는 터미널을 열어야 한다. 대부분의 소상공인에게 그건 이 도구를 못 쓰게 되는 이유다. 그래서 **"화려하게"를 React 조건으로 삼지 않는다**고 명시했다. 안 그러면 다음 세션의 에이전트가 "인터랙티브하게 해줘" 한마디에 Vite 프로젝트를 만들어놓는다.

React로 갈 때는 STEP 1-4 질문에 터미널 비용을 미리 고지하고 "기능을 줄이고 더블클릭으로 열리게" 대안을 같이 준다.

## 설치가 첫 번째 이탈 지점이었다

기존 `setup.sh`는 파서만 깔고 *"Claude Code에 플러그인으로 등록하려면 이 폴더 경로를 쓰세요"*라고만 했다. **방법을 안 알려준다.** 바이브 코딩 처음 하는 사람은 여기서 멈춘다.

그래서 setup이 등록까지 하도록 바꿨다. `~/.claude`와 `~/.codex`를 찾아서 명령·스킬·서브에이전트 6종을 넣는다. 넣을 때 `{ROOT}`와 `${CLAUDE_PLUGIN_ROOT}` 자리를 **저장소 절대경로로 치환**한다. 슬래시 명령은 `${CLAUDE_PLUGIN_ROOT}`를 못 받기 때문에 이 치환이 없으면 경로가 깨진다.

**Windows에서 함정이 하나 있었다.** 이 환경의 Git Bash `$HOME`이 `/c/Users/gjcks/AppData/Roaming/SPB_Data`였다. 실제 홈이 아니다. 그대로 뒀으면 엉뚱한 데 설치하고 "성공"이라고 출력했을 것이다. `%USERPROFILE%`를 우선하도록 고쳤다.

설치 후 실측으로 확인했다. 파일 9개 생성, 플레이스홀더 잔여 0건, PLAYBOOK 절대경로 실재, 그리고 **같은 세션에서 `design-on` 스킬이 실제로 잡혔다.**

## 개명 — design-on

`designon`을 `design-on`으로 바꿨다. 저장소·폴더·파일명·frontmatter·명령어 전부. 127곳이었고 대소문자 변형은 없었다(전수 확인 후 sed).

GitHub은 rename 후 구주소를 자동 리다이렉트하므로 이미 클론한 사람도 깨지지 않는다. 다만 **로컬 설치 파일이 절대경로를 굳혀 쓰기 때문에** 폴더를 옮기거나 이름을 바꾸면 `setup`을 다시 돌려야 한다. README와 setup 출력 양쪽에 적었다.
