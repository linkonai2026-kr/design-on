# 변경 이력

design-on의 버전별 변경 사항입니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 씁니다.

날짜는 작업이 저장소에 반영된 날 기준입니다.

---

## [4.1.0] — 2026-08-06

### 이번 버전의 요점

**결과물이 매번 달라집니다.** 색과 글꼴은 바뀌는데 구조는 늘 똑같던 문제를 고쳤습니다. 그리고 팔레트를 고를 때 **본문이 안 읽히는데도 아무 말 없이 통과시키던 버그**를 잡았습니다.

### 추가

- **페이지 구조 아키텍처 10종** (`data/layouts.json`, `pick.mjs layouts`)
  - 에디토리얼 단 · 전면 사진 연속 · 목차 주도 · 가로 스트립 · 고정 분할 · 포스터 · 시간축 · 카탈로그 · 문서형 · 공간 주도
  - 색만 바꾼 변주가 아니라 **구조가 실제로 다릅니다.** `목차 주도`는 히어로가 없고, `문서형`은 사진이 0장이고, `포스터`는 스크롤이 거의 없습니다
  - 치수(`maxWidth`·`gutter`·`sectionGap`)가 아키텍처마다 다릅니다
  - **업종이 아니라 이 가게가 가진 자산이 구조를 정합니다.** 같은 카페라도 공간 사진이 자산인 집과 원두 목록이 자산인 집은 다른 구조가 나옵니다
- **PLAYBOOK STEP 2-0 — 구조를 먼저 고른다.** 아트 디렉션 카드에 `구조`와 `고른 이유` 필드를 넣었습니다. 이유를 한 줄로 못 쓰면 안 고른 것으로 봅니다
- **교체 테스트.** *상호명과 사진만 다른 업종 것으로 바꿔도 이 페이지가 그대로 말이 되는가?* 되면 실패로 판정합니다. 안티패턴 디텍터 59종은 전부 요소 단위라 이걸 못 잡습니다
- **"이 페이지에만 있는 것을 하나 만든다"** 규칙. 카페면 지금 문 열었는지 실시간 표시, 세무사무소면 이번 달 신고 마감일 같은 것. 대부분 JS 열 줄이면 됩니다
- **네 번째 예시 [`examples/tax/`](examples/tax/index.html)** — 세무사무소, `문서형` 구조. **사진 0장, 히어로 없음**, 본문 세리프 + 제목 산세리프, 본문 폭 720px. 기존 셋이 전부 "히어로 + 섹션"이라 새 구조를 증명할 실물이 필요했습니다
- **라틴 서체 폴백 5종** (`fonts.json`의 `latinFallback`) — Public Sans · Libre Franklin · Work Sans · Figtree · Mulish. 전부 Google Fonts입니다
- **무료 목업 사이트 안내** (STEP 8-3). "이 가게 로고가 박힌 컵"은 스톡에 없고 AI로 만들면 로고가 뭉개집니다. 사진 폴백에서 유일하게 안 걸리던 구멍입니다
- **컬러 팔레트 5세트** 추가 (78 → 83세트)
- **디자인 툴 11개** 추가 (883 → 894개)
- **회귀 테스트 4종** — 아키텍처 간 치수가 실제로 다른지, 고정 치수와 업종 순서표가 되살아나지 않았는지, 라틴 폴백이 살아 있는지, 대비 미달 팔레트가 경고를 내는지

### 수정

- **`pick.mjs`가 본문 대비 미달을 경고하지 않던 문제.** `muted` 없음과 `accent` 미달만 검사하고 **정작 제일 중요한 본문/배경 대비는 빠져 있었습니다.** DB 68세트 중 17세트(25%)가 조용히 통과하고 있었습니다. PLAYBOOK은 "대비 계산까지 끝내서 돌려준다"고 약속하는데 그 약속이 제일 중요한 항목에서 깨져 있었습니다
- **동일화의 진원지였던 고정 치수 제거.** STEP 4에 박혀 있던 `최대 폭 1180px` · `여백 20/48px` · `섹션 간격 clamp(64px,10vw,128px)` · `히어로 78vh` · `텍스트:이미지 5:7`. 나쁜 값은 아니지만 **모든 페이지가 이 한 벌을 입었습니다**
- **업종별 고정 섹션 순서표 제거.** "카페니까 히어로-소개-메뉴-위치"는 구조를 고른 게 아니라 안 고른 것입니다. 대신 "방문자가 궁금한 것"을 세우고 1순위가 첫 화면이 되게 했습니다
- **`commands/design-on.md`에 STEP 2-0이 빠져 있던 문제.** `/design-on`이 실제로 읽는 파일인데 가장 중요한 규칙이 없었습니다. `description`도 "병렬 7트랙"으로 실제 흐름과 어긋나 있었습니다
- **경로 표가 원본 JSON을 안내하던 문제.** `data/palettes.json`·`data/tools.json` 경로를 알려주면서 동시에 "통째로 읽지 마라"고 하고 있었습니다. `pick.mjs`로 바꿨습니다

### 변경

- **README 순서를 뒤집었습니다.** 설치가 470번째 줄에 있어서 처음 온 사람이 변경 이력 460줄을 먼저 읽어야 했습니다. 설치와 쓰는 법을 맨 위로 올리고 버전 이력을 뒤로 내렸습니다
- **PLAYBOOK 머리에 "건너뛰면 안 되는 다섯 가지"** 표를 넣었습니다. 1,000줄이 넘어서 읽다가 실을 놓칠 수 있습니다
- STEP 4의 절 순서를 빌드 순서대로 재배열했습니다. 금지 목록이 치수보다 앞에 있어 출발점이 묻혀 있었습니다
- 사진 트랙에 C-3-1 추가 — **비슷한 남의 제품 사진을 억지로 넣지 마라.** 그럴 바엔 타이포 주도로 갑니다

### 넣지 않기로 한 것

판단 근거는 [`CREDITS.md`](CREDITS.md)에 적어 두었습니다.

- **A/B 테스트 도구** (LaunchDarkly · GrowthBook · Statsig · Coframe · Unrevealed) — `tools.json`에는 넣었지만 PLAYBOOK 어디에도 배선하지 않았습니다. 트래픽과 서버가 있어야 성립하는데, 월 방문자 서른 명인 동네 가게 페이지에서는 통계적으로 의미가 없습니다
- **페이지 생성 AI** (PageAI · Pen.dev · UX Pilot) — design-on과 목적이 겹치는 경쟁 도구라 디렉터리 기록만 했습니다

---

## [4.0.0] — 2026-07-29

### 이번 버전의 요점

**만들고 끝내지 않습니다.** 예전에는 `<!-- 실제 정보로 교체하세요 -->` 주석을 남기고 끝냈습니다. HTML을 만지기 싫어서 이 도구를 받은 사람에게 HTML을 만지라고 시키는 셈이었습니다.

### 추가

- **STEP 8 — 완성 후 대화.** 여기까지가 완성입니다
  - 마음에 드는지 묻습니다 (A 좋다 / B 몇 군데 / C 아니다)
  - **페이지에 실제로 들어간 가짜 값만** 빈칸 채우기로 묻고 모든 페이지에 반영합니다
  - 사진은 자리마다 뭐가 보이는지 적고 하나씩 확인합니다. 바꾸겠다고 하면 **어떻게 바꿀지 먼저 물어본 뒤에** 손댑니다
  - C를 고르면 HTML을 고치지 않고 STEP 2 아트 디렉션으로 되감습니다 (1회)
  - 최대 2라운드로 제한합니다
- **React + Vite 출력.** 단일 HTML · 멀티페이지에 이어 세 번째 형태입니다. 다만 **화면에 상태가 여러 개 얽힐 때만** 씁니다. "화려하게", "3D로"는 조건이 아닙니다
- **설치 자동화.** `setup` 스크립트가 검사기뿐 아니라 `~/.claude`·`~/.codex`를 찾아 슬래시 명령·스킬·서브에이전트 6종까지 등록합니다

### 변경

- **이름이 `designon`에서 `design-on`으로 바뀌었습니다.** 저장소·폴더·파일명·frontmatter·명령어 전부입니다. 예전 GitHub 주소는 자동으로 리다이렉트되지만, **이미 받아두셨다면 `setup`을 다시 실행해 주세요**
- STEP 6의 "루프를 돌지 마라"와 STEP 8이 충돌하지 않는 이유를 명시했습니다. 전자는 에이전트의 자가 QA 루프를 막는 규칙이고, 사람에게 묻는 것은 추측을 사실로 바꾸는 일입니다

### 넣지 않기로 한 것

- **Astryx** (Meta) — 처음에는 "빌드 도구가 필요해서" 제외했는데 그 논거는 React 지원과 함께 무효가 됐습니다. 다시 열어 본 결론은 **목적이 반대**라는 것입니다. Astryx는 어느 화면을 만들어도 같은 제품처럼 보이게 하는 시스템이고, design-on은 매번 다르게 보여야 합니다

---

## [3.0.0] — 2026-07-29

### 추가

- **조회 CLI `scripts/pick.mjs`.** `data/*.json` 통째 읽기를 대체합니다. 실측 기준 **91,885 토큰 → 1,621 토큰(98% 절감)**. `tools.json` 하나가 262KB, 약 87,000 토큰이라 한 번 읽으면 세션 예산이 사라집니다
- **[Fontshare](https://www.fontshare.com/) 라틴 서체.** 한글 서체는 숫자와 영문이 약한 경우가 많아 `1,280도`·`02-1234-5678` 같은 표기에서 티가 납니다
- **업종별 사진 프롬프트 레시피** (`data/photo-recipes.json`) 7종
- **3D·인터랙티브 분기.** 요청하면 켭니다. L1 CSS만 / L2 바닐라 Canvas / L3 Three.js CDN / L4 Spline 임베드. 빌드 도구 없이 `importmap`으로 붙입니다
- 예시 3종을 **일부러 완전히 다른 디자인**으로 재생성했습니다. `examples/studio/`가 Three.js 3D 기물 뷰어 예시입니다

---

## [2.0.0] — 2026-07-29

### 이번 버전의 요점

v1의 문제는 하나였습니다. **결과물에서 AI 티가 났습니다.** 원인은 셋이었습니다 — 검증이 없었고, 디자인 방향을 정하는 단계가 없었고, 혼자 순서대로 다 했습니다.

### 추가

- **안티패턴 디텍터 59종** ([Impeccable](https://github.com/pbakaus/impeccable), Apache-2.0). 생성 후 반드시 돌리고 잡힌 건 고칩니다. **지켰는지를 AI가 스스로 판단하면 언제나 "지켰다"고 답합니다**
- **아트 디렉션 단계** (STEP 2). 모드·3다이얼·팔레트 역할·서체 페어링을 먼저 확정합니다
- **제작 4트랙 · 검수 3트랙 병렬 실행.** 각 트랙은 아트 디렉션 카드 하나만 보고 자기 일만 합니다
- **멀티페이지 지원.** 원페이지는 기본값이지 제약이 아닙니다
- 하위 지식 4종 벤더링 — Impeccable · [tasteskill](https://github.com/Leonxlnx/taste-skill) · [UI Skills](https://github.com/ibelick/ui-skills) · [emilkowalski/skill](https://github.com/emilkowalski/skill)

---

## [1.0.0] — 2026-07-28

- 초기 버전 (`design-kit`). 질문 우선 흐름과 컬러 팔레트·디자인 툴 DB

[4.1.0]: https://github.com/linkonai2026-kr/design-on/compare/v4.0.0...v4.1.0
[4.0.0]: https://github.com/linkonai2026-kr/design-on/compare/v3.0.0...v4.0.0
[3.0.0]: https://github.com/linkonai2026-kr/design-on/compare/v2.0.0...v3.0.0
[2.0.0]: https://github.com/linkonai2026-kr/design-on/compare/v1.0.0...v2.0.0
[1.0.0]: https://github.com/linkonai2026-kr/design-on/releases/tag/v1.0.0
